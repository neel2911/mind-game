export type SettingsType = {
    playerName: string,
    difficulty: string
}

export type StatsType = {
    score: string
    highScore: string;
}

export type CardType = {
    id: number,
    url: string
}
